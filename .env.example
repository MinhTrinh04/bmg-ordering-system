# --- SERVER CONFIG ---
SERVER_PORT=8080

# --- DATABASE CONFIG ---
MONGO_URI=mongodb://localhost:27017/bmg_ordering
MONGO_DB_NAME=bmg_ordering

# --- SECURITY & CORS (Quan trọng cho Frontend kết nối) ---
# Điền domain của frontend vào đây (ví dụ http://localhost:3000)
# Dùng dấu phẩy để ngăn cách nếu có nhiều domain
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# --- RATE LIMITING (Chống Spam) ---
# Số lượng request tối đa trong 1 khoảng thời gian
RATE_LIMIT_CAPACITY=1
# Số token hồi phục sau mỗi chu kỳ
RATE_LIMIT_REFILL_TOKENS=1
# Chu kỳ hồi phục (giây)
RATE_LIMIT_REFILL_DURATION=10

# --- WEBSOCKET CONFIG ---
WS_ENDPOINT=/ws-ordering
WS_BROKER_PREFIX=/topic
WS_APP_PREFIX=/app